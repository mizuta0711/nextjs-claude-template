# Stage 2 設計レビュー結果

- 実施日時: 2026-08-12 10:14
- 対象: [docs/features/20260812_ハーネス構成刷新_Phase3.md](../features/20260812_ハーネス構成刷新_Phase3.md)
- モード: tech (Stage 2)

## 総合判定: ⚠️ 条件付き承認

技術設計の骨格（移設行範囲・削除74行の見積もり・paths 設計・フックの非ブロッキング方針）は
**実ファイル照合で正確であることが確認された**。一方、フックの発火条件に1件の実装バグ相当の欠陥、
リネーム対象の漏れ1件、設計セクションの欠落1件があるため、修正のうえ実装に進む。

- 要修正: 重要 3件 / 中 3件 / 低 2件
- 競合・要判断: なし（新たに判明した確認事項が1件）

---

## code-reviewer（技術設計の整合性）

| # | 重要度 | 内容 | 対象 | 対応状況 |
|---|--------|------|------|---------|
| 1 | 重要 | PostToolUse の matcher が `Edit\|Write` なのに `if` が `Edit(...)` のみ。`Write` で新規 `.tsx` を作成した場合にフックが発火せず、受け入れ条件「`.ts`/`.tsx` を編集したとき lint が自動修正される」を満たせない | 4-5-1 | ✅ 修正済 — `Edit` 用と `Write` 用の2エントリに分割 |
| 2 | 重要 | `code-review/SKILL.md:48` の出力ファイル名 `YYYYMMDD_HHMMSS_code-review.md` がリネーム対象から漏れている。しかも 3-4(3) の grep 検証は `docs/reviews/` を除外するため**検出できない** | 4-6 | ✅ 修正済 — 手順2に追加し、検証手順にも SKILL.md 内の文字列確認を追加 |
| 3 | 中 | タスク 3-F（派生プロジェクト適用プロンプトへの追記）の技術設計が Stage 2 に存在しない。受け入れ条件12項目目に対応する設計が欠落したまま実装に入ることになる | 4-7 | ✅ 修正済 — 4-7 に 3-F の設計を追加 |
| 4 | 中 | `03_code-reviewシーケンス図.md` の出現数が「6」と記載されているが実測 **8** | 4-1 / 4-6 / 7章 | ✅ 修正済 — 3箇所すべて訂正 |
| 5 | 中 | `バイブコーディング運用ガイド.md` の出現数が「9」と記載されているが実測 **8** | 4-1 / 7章 | ✅ 修正済 |
| 6 | 中 | `運用ガイド.md:31` のディレクトリ構成図に `browser-test-checklist.md`（旧パス）参照があるが、3-D の手順に含まれていない | 4-7 (3-D) | ✅ 修正済 — 3-D の対象に追加 |
| 7 | 低 | PreCompact の `additionalContext` は公式ドキュメントで**非対応と明記**されている。「仕様上未確認」という保留表現は確定情報に更新できる | 4-5-2 | ✅ 修正済 — 確定情報に更新。ファイル退避方式で確定 |
| 8 | 低 | `CLAUDE.md:5,7` に `<!-- BEGIN:nextjs-agent-rules -->` / `<!-- END:nextjs-agent-rules -->` という既存マーカーがあるが、設計書が言及していない | 4-4 | ✅ 対応方針を追記 — 下記参照 |

### 検証メモ（メイン側で再確認した内容）

すべての指摘についてメイン側で裏取りを実施した。

**出現数の実測**（`grep -o '/code-review' <file> | wc -l`）:

| ファイル | 設計書の記載 | 実測 | 判定 |
|---------|------------|------|------|
| `docs/guide/バイブコーディング運用ガイド.md` | 9 | **8** | ❌ 要訂正 |
| `docs/guide/バイブコーディング入門ガイド.md` | 8 | 8 | ✅ |
| `docs/diagrams/03_code-reviewシーケンス図.md` | 6 | **8** | ❌ 要訂正 |
| `.claude/01_development_docs/09_開発フローと規模判定.md` | 3 | 3 | ✅ |
| `docs/guide/オプションMCP追加ガイド.md` | 2 | 2 | ✅ |
| `README.md` | 2 | 2 | ✅ |
| `CLAUDE.md` | 1 | 1 | ✅ |
| `docs/features/TEMPLATE.md` | 1 | 1 | ✅ |
| **合計** | 32 | **33** | ❌ 要訂正 |

指摘 #4 #5 は正しい。当初の「約32箇所」は grep の**行数**を数えたもので、
同一行に複数出現するケースを取りこぼしていた。

**指摘 #2 の裏取り**: `.claude/skills/code-review/SKILL.md` 内の `code-review` 出現は
2行目（`name:`）と48行目（出力ファイル名）の2箇所。48行目は設計書の手順から漏れていた。

**指摘 #8 の裏取り**: マーカーは実在する。ただし **HTML コメントはコンテキスト注入前に除去される**ため、
Claude からは見えず、トークンも消費しない。Phase 1 で `AGENTS.md` への参照を削除した経緯から、
このマーカーは AGENTS.md との同期用に置かれた**残骸である可能性が高い**。

> **対応方針**: 3-A では**マーカーに触らない**。用途が確定していないものを構成刷新のついでに消すと、
> 外部ツールが依存していた場合に切り分けが困難になる。Phase 4 で用途を確認したうえで削除可否を判断する。
> 設計書の注意事項にその旨を明記した。

---

## 該当なしと判断した観点

通常の Stage 2 レビュー観点のうち、本 Phase の性質上該当しないもの:

- API 設計のレスポンス構造 ⇔ DB 設計のテーブル定義: **該当なし**（アプリの API・DB を変更しない）
- サービス設計の戻り値型 ⇔ API 設計のレスポンス: **該当なし**
- リクエスト・レスポンス JSON 例の網羅性: **該当なし**
- ページネーションキーの `items` 統一: **該当なし**

---

## 改訂履歴

| 版数 | 日付 | コミット | 内容 | 担当 |
|------|------|---------|------|------|
| 1.0 | 2026-08-12 | (未確定) | 初版作成（Stage 2 レビュー結果） | Claude |
