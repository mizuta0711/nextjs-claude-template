# 派生プロジェクトへの Phase 1 適用プロンプト（2026-08-12）

本ファイルは、nextjs-claude-template の **Phase 1（Claude Code 仕様との不整合バグ修正）** を、
本テンプレートから派生した各アプリケーションプロジェクトに反映するための指示書。

## 対象プロジェクト（想定）

- `D:/Develop/Web/skillup_mock/`
- `D:/Develop/Web/EngineerPotal/engineer-potal/`
- `D:/Develop/Web/AIStory/`
- その他、本テンプレートの `.claude/skills/` `.claude/agents/` `.claude/hooks/` を取り込んだプロジェクト

> 本 Phase の修正は **Claude Code のハーネス設定レベル**の不具合修正であり、
> Next.js のバージョンやアプリの実装内容には依存しない。Next.js 15 系のプロジェクトにもそのまま適用できる。

## 使い方

1. 対象プロジェクト側で新しい Claude Code セッションを起動する
2. 下記「## Claude Code への指示プロンプト」のコードブロック内を、そのまま貼り付けて実行させる
3. 完了後、必ず**メイン側で差分を確認**してからコミットする（CLAUDE.md「サブエージェント運用ルール」に準拠）

## 関連コミット（nextjs-claude-template 側）

- Phase 1: `4ea1985` Claude Code 現行仕様との不整合を修正

## 背景

Claude Code 本体の仕様がテンプレート作成時から変化しており、以下が現在の仕様と食い違っていた。
詳細な調査結果は [20260812_テンプレート最新化レビュー.md](./20260812_テンプレート最新化レビュー.md) を参照。

| # | 内容 | 影響 |
|---|------|------|
| 1 | SKILL.md 本文の `$0` がスキル引数置換と衝突 | `/update-docs` が**必ず失敗する** |
| 2 | SKILL.md のフロントマター欠落 | Claude がスキルの用途を判断できない |
| 3 | `user_invocable` はフィールド名の誤り（正: `user-invocable`） | 設定が無視される |
| 4 | README / ガイドが実態とズレ | 人・AI 双方が誤った前提で作業する |
| 5 | フックスクリプトのコメントが誤情報 | 将来の改修判断を誤らせる |

Phase 2 以降（権限設定の共有化、フックの `if` 化、CLAUDE.md の `.claude/rules/` 分割等）は本指示書の対象外。
テンプレート側で実施後、別途指示書を出す。

---

## Claude Code への指示プロンプト

```
nextjs-claude-template（パス: D:/Develop/Web/nextjs-claude-template/）で、
Claude Code の現行仕様との不整合バグ修正（Phase 1）が実施された。
本プロジェクトにも同等の修正を反映してほしい。

参照元:
- レビュー詳細: D:/Develop/Web/nextjs-claude-template/docs/reviews/20260812_テンプレート最新化レビュー.md
- 修正後の実物: D:/Develop/Web/nextjs-claude-template/.claude/skills/ および .claude/hooks/

## 大前提

本プロジェクトはテンプレートを適用した後に独自進化している可能性がある。
以下は**絶対に上書きしないこと**:

- CLAUDE.md のプロジェクト概要・スタック情報・独自ルール
- `.claude/01_development_docs/` `.claude/02_design_system/` のプロジェクト固有設計方針
- 既存の機能設計書 (`docs/features/`) と設計書 (`docs/設計書/`)
- テンプレートには無い独自のスキル・エージェント・フック
- スキル本文のうち、プロジェクト固有のチェック項目・パス・コマンド

本作業は「**Claude Code の仕様に対して壊れている記述だけを直す**」ものであり、
テンプレートの内容を丸ごと同期する作業ではない。

## 作業 1: 全 SKILL.md のフロントマター検査（最重要）

`.claude/skills/*/SKILL.md`（および存在すれば `.claude/commands/*.md`）を**全件**開き、
以下の3点を検査して該当するものだけ修正する。テンプレート由来でない独自スキルも対象。

### 1-A. フロントマターの欠落

ファイル先頭が `---` で始まっていないスキルは、`description` が本文1段落目からの
フォールバックになり、Claude の起動判断が不安定になる。以下を先頭に追加する:

---
name: <ディレクトリ名と同じ>
description: <何をするスキルか。「いつ使うか」を先頭に書く>
---

テンプレート側では `.claude/skills/done/SKILL.md` がこれに該当した。修正後の姿:

---
name: done
description: 作業完了時に、決められたテーブル形式で完了報告を出力する。S/M/Lフローの最終ステップ。
---

注意: `done` に `disable-model-invocation: true` は**付けないこと**。
CLAUDE.md の S/M/L フローで Claude 自身が最終ステップとして実行するスキルであり、
これを付けると Claude から起動できなくなりフローが成立しない。

### 1-B. アンダースコア区切りのフロントマターキー

Claude Code のフロントマターは**ハイフン区切り**が正。
アンダースコア区切りのキーは不正キーとして無視される。

| 誤 | 正 |
|----|-----|
| `user_invocable` | `user-invocable` |
| `allowed_tools` | `allowed-tools` |
| `argument_hint` | `argument-hint` |
| `disable_model_invocation` | `disable-model-invocation` |

テンプレート側では `complete-feature/SKILL.md` に `user_invocable: true` があった。
ただし `user-invocable` の既定値は `true` なので、**書き直すのではなく行ごと削除**する。
そのうえで、ユーザーが明示的に呼ぶべき完了処理スキルであるため以下に置き換えた:

disable-model-invocation: true

このフィールドを付けてよいのは「**Claude が自律的に走らせると困るスキル**」に限る。
本プロジェクトでの判断基準:
- 付ける: 完了処理、リリース処理、破壊的操作を伴うもの
- 付けない: CLAUDE.md のフローで Claude が実行するステップになっているもの

### 1-C. SKILL.md 本文中の `$0` / `$1` / `$2`

これが最も実害が大きい。Claude Code は SKILL.md 本文の `$0` `$1` `$2` を
**スキル引数の置換プレースホルダ**（`$ARGUMENTS[0]` 等の短縮形）として展開する。
そのため、bash のつもりで書いた `$0` はコードブロック内であっても壊れる。

テンプレート側の `update-docs/SKILL.md` にあった実例:

git diff --name-only HEAD~${0:-1}

これは引数ありなら意図しない文字列に置換され、引数なしなら未置換のまま bash に渡り、
bash の `$0`（シェル名）が入って `HEAD~bash` になる。**どちらでも必ず失敗する。**

修正方針は、フロントマターで名前付き引数を宣言し、本文では `$名前` を使う:

（フロントマター）
argument-hint: "[遡るコミット数（省略時は1）]"
arguments: [depth]

（本文）
`$depth` に数値が入っていればその値を、空文字なら `1` を使って以下を実行する:

git diff --name-only HEAD~1

全スキルを `grep -rn '\$[0-9]' .claude/skills/` で走査し、
bash 変数のつもりで書かれている箇所をすべて洗い出すこと。

## 作業 2: フックスクリプトのコメント訂正

`.claude/hooks/*.js` に以下の趣旨のコメントがあれば、それは**現在は誤り**:

「Claude Code の `if` フィールドは公式仕様ではないため、コマンド判定は
  本スクリプト内で stdin の JSON (tool_input.command) を読んで行う。」

`if` はパーミッションルール構文を使う公式フィールドとして正式サポートされており、
`PreToolUse` / `PostToolUse` / `PostToolUseFailure` / `PermissionRequest` / `PermissionDenied`
で評価される。テンプレート側では以下の表現に差し替えた:

 * コマンド判定は本スクリプト内で stdin の JSON (tool_input.command) を読んで行う。
 * settings.json 側で `if: "Bash(git commit *)"` を併用すれば無関係な Bash 呼び出しで
 * 本スクリプトが起動しなくなる（`if` はパーミッションルール構文の公式フィールド）。
 * ここでの判定は `if` の有無に関わらず動くようにするための二重防御。

**この作業ではコメントのみ直すこと。** settings.json への `if` 追加は Phase 2 の対象であり、
今回は実施しない（フックの発火挙動を変える変更のため、別途まとめて検証する）。

スクリプト内の判定ロジック自体は二重防御として有用なので、削除しないこと。

## 作業 3: README とガイド類の実態合わせ

本プロジェクトの README.md（および `docs/guide/` 配下）を読み、
記載内容と実ファイルの差分を洗い出して修正する。テンプレート側で見つかった典型的なズレ:

1. **存在しないファイルへの言及** — 例: `AGENTS.md`
   Claude Code は AGENTS.md を読まない仕様。他エージェント（Codex / Cursor 等）と
   共有したい場合のみ AGENTS.md を実体とし、CLAUDE.md 冒頭に `@AGENTS.md` と書いて
   import する（Windows ではシンボリックリンクに管理者権限が要るため import 一択）。
   その予定がないなら、言及ごと削除する。

2. **存在しないエージェントへの言及** — 例: `project-manager`
   `.claude/agents/` の実ファイル一覧と突き合わせる。

3. **スキル一覧の欠落**
   `.claude/skills/` の実ディレクトリ一覧と突き合わせ、抜けを追加する。

4. **フロー図の陳腐化**
   README のフロー記述が CLAUDE.md の S/M/L フロー・2段階設計と一致しているか確認する。
   CLAUDE.md を正とし、README 側を合わせる。

## 検証

修正後、以下を実行して報告すること:

1. `grep -rn '\$[0-9]' .claude/skills/` — bash 変数のつもりの箇所が残っていないか
2. `grep -rn '_invocable\|allowed_tools\|argument_hint' .claude/` — アンダースコア記法の残存
3. 全 SKILL.md の先頭3行を出力し、フロントマターが揃っているか目視確認
4. `.claude/hooks/*.js` があれば `node --check` で構文確認
5. 実際に `/update-docs` を引数なしで起動し、Step 1 のコマンドが正しく走るか確認
6. `/doctor` を実行し、設定の警告が出ていないか確認

## 報告

CLAUDE.md の「作業完了時の報告フォーマット」に従い、テーブル形式で報告すること。
加えて、以下を明記する:

- 検査した SKILL.md の総数と、実際に修正した件数
- 本プロジェクト固有スキルで見つかった同種の不具合（テンプレートには無かったもの）
- 適用を見送った項目とその理由

コミットは差分確認後に行い、プッシュはユーザーの指示を待つこと。
```

---

## 適用状況トラッキング

| プロジェクト | 適用日 | コミット | 備考 |
|-------------|--------|---------|------|
| skillup_mock | - | - | 未適用 |
| EngineerPortal | - | - | 未適用 |
| AIStory | - | - | 未適用 |

---

## 改訂履歴

| 版数 | 日付 | コミット | 内容 | 担当 |
|------|------|---------|------|------|
| 1.0 | 2026-08-12 | - | 初版作成（Phase 1 適用指示書） | Claude |
