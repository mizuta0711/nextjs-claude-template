# 設計書同期トラッキング

コミットごとに設計書の同期状態を記録する。
`/update-docs` 実行時に自動追記される。プッシュ前チェックは `grep` でコミットハッシュの存在を確認するのみ。

## ルール

- `/update-docs` 実行時: 対象コミットのハッシュ・同期先・判定を追記
- 設計書更新不要なコミット（バグ修正等）: `更新不要` として記録
- プッシュ前チェック: `git log --format=%h @{upstream}..HEAD` の全ハッシュがこのファイルに存在するか `grep` で確認

## 同期記録

| コミット | 日付 | 同期先 | 判定 |
|---------|------|--------|------|
| `4ea1985` | 2026-08-12 | 更新不要（Claude Code ハーネス設定の修正のみ。API・サービス・スキーマの変更なし。記録は `docs/reviews/20260812_テンプレート最新化レビュー.md`） | ✅ |
| `9687846` | 2026-08-12 | 更新不要（レビュー文書の追加のみ） | ✅ |
| `a4be874` | 2026-08-12 | 更新不要（Claude Code 設定・フック・スキル/エージェント定義の変更のみ。アプリ実装の変更なし） | ✅ |
| `3e07fa2` | 2026-08-12 | 更新不要（レビュー文書・適用指示書の更新のみ） | ✅ |
| `3225ec1` | 2026-08-12 | 更新不要（Claude Code の権限設定変更のみ） | ✅ |
